<%# STORAGE DAEMON HELPER TEMPLATE %>
# Generated by Chef for <%= node['bareos']['storage']['name'] %>
# Local modifications will be overwritten.
#
# Bareos Storage Daemon Helper Configuration File

# This is a base file so the recipe works with no additional help.
# You can also include a list of strings to assist with additonal config
# inclusions or other bits not currently available in the base storage daemon template

<% unless @sd_help.nil? %>
  <% @sd_help.sort.each do |helper_name, helper_str| %>
# <%= helper_name %>
<%= helper_str %>

  <% end %>
<% end %>
