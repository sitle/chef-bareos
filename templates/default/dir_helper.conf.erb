<%# DIRECTOR HELPER TEMPLATE %>
# Generated by Chef for <%= node['bareos']['storage']['name'] %>
# Local modifications will be overwritten.
#
# Bareos Director Helper Configuration File

# This is a base file so the recipe works with no additional help.
# You can also include a list of strings to assist with additonal config
# inclusions or other bits not currently available in the base director template

<% unless @dir_help.nil? %>
  <% @dir_help.sort.each do |helper_name, helper_str| %>
# <%= helper_name %>
<%= helper_str %>

  <% end %>
<% end %>
